{
  "name": "coastwide-exterior-cleaning",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "node scripts/prepare-deploy.mjs",
    "verify": "node scripts/verify-deploy.mjs",
    "test:form": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
